type Query{
    hello: String!
    users:[User]
    userById(id: ID!): User

    posts: [Post]
    postById(id: ID!): Post
}

type Post {
    id: ID!
    title: String!
    author: User
}



type User{
    id: ID!
    name: String!
    email: String

    posts:[Post]
}